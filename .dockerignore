# =============================================================================
# .dockerignore for NGÜ BVS App
# =============================================================================
# This file prevents unnecessary files from being copied into Docker images,
# reducing build time and image size.
# =============================================================================

# -----------------------------------------------------------------------------
# Development & Documentation
# -----------------------------------------------------------------------------
do_not_deploy/
README.md
CLAUDE.md
*.md
!app-deployment/README.md

# -----------------------------------------------------------------------------
# Version Control
# -----------------------------------------------------------------------------
.git/
.gitignore
.gitattributes

# -----------------------------------------------------------------------------
# Python Development
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.pyc
venv/
env/
.venv/
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
*.egg

# -----------------------------------------------------------------------------
# Environment & Secrets
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.cert

# -----------------------------------------------------------------------------
# IDE & Editor Files
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.code-workspace
*.swp
*.swo
*~

# -----------------------------------------------------------------------------
# OS Files
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# Runtime & Generated Files
# -----------------------------------------------------------------------------
flask_session/
logs/
*.log
uploads/
certificates/*.pdf
certificates/*.png

# -----------------------------------------------------------------------------
# Database Files
# -----------------------------------------------------------------------------
*.db
*.sqlite
*.sqlite3
*.sql
*.sql.gz
*.dump

# -----------------------------------------------------------------------------
# Backup Files
# -----------------------------------------------------------------------------
*.backup
*.bak
*.old
backups/
db-backups/

# -----------------------------------------------------------------------------
# Docker-specific Files (shouldn't be in build context)
# -----------------------------------------------------------------------------
Dockerfile
docker-compose*.yml
.dockerignore

# app-deployment: Exclude large/sensitive subdirectories
# Keep Dockerfile (needed for build) but exclude runtime data
app-deployment/postgres-data/
app-deployment/certbot/
app-deployment/logs/
app-deployment/.env
app-deployment/nginx/ssl/
app-deployment/*.sql
app-deployment/backup_*.sql

# -----------------------------------------------------------------------------
# Large directories NOT needed in container (mounted as volumes at runtime)
# -----------------------------------------------------------------------------
# certificates/ is mounted via docker-compose volume, no need to copy
certificates/

# Database migrations (not needed in container)
migrations/

# -----------------------------------------------------------------------------
# IMPORTANT: Files needed in container
# -----------------------------------------------------------------------------
# ⚠️ ACHTUNG: Neue Python-Dateien müssen hier explizit hinzugefügt werden!
# Wenn du eine neue .py Datei erstellst, füge sie unten mit !dateiname.py hinzu
# -----------------------------------------------------------------------------
# Python application files
!app.py
!models.py
!stripe_service.py
!pdf_service.py
!email_service.py
!hubspot_service.py
!certificate_manager.py
!book_names.py
!vectorize.py
!setup_db.py
!send_daily_report.py
!backfill_payment_methods.py
!requirements.txt

# Static and template files
!static/**
!templates/**

# Data files
!verses.json

# Routes (if exists)
!routes/**